# ModelSpine 代码仓库协作规则

先读 README.md、docs/architecture.md，再读目标模块的 module.json、当前合同和设计合同。当前范围以实际源码与验收为准；设计、实现、实验观察和受支持结论分开，比较优势尚未证明。

- 用户最新任务决定范围；完整设计九模块的责任和演化，再按真实消费者实现最小完整行为。不预设应用试点，已有订单材料只是工程夹具。
- 包按能力命名。共享值对象及跨包接口归 protocols，已接受模型写权归 model-kernel；其他能力产生候选或报告，应用入口负责装配。
- runtime_dependencies 记录当前运行导入，depends_on 记录目标设计依赖；planned 模块没有运行依赖声明。运行包不得导入 apps、studies、测试、研究材料或旧工作树。
- 实验编排与条件选择放 studies；领域定义放 domain-packs；外部后端适配放 adapters。通过公开合同注入检查器等实现，不能把论文条件开关放入生产包。
- 用户意图、实现事实和候选解释分别维护。未知、错误、缺失、超时和不适用不得写成检查通过。失败不得静默切换模型、数据源、检查器或削弱验收。
- 校验输入、权限、版本、候选绑定及完整性，并由明确的边界负责。修复改善、模型保存、业务许可与应用交付分别判定。
- 从仓库根运行 `python -B run_tests.py`，按改动验证有意义的反例；工程验收不证明研究比较优势。更新源码时同步合同、模块 manifest 和相关公开说明。
- 收尾审查 diff：新增配置、依赖、分支和抽象须有当前消费者或正确性责任，保留失败的真实结果。不因为“以后可能用到”扩展实现。
- 代码版本使用 Git 提交；工作树只维护当前代码，不新增备份目录、源码 ZIP 或重复历史版本。删除来源前先核对引用与已保存的 Git 版本；不可改写历史失败证据。
- 此仓库不包含私人稿件、回复信、参与者原始数据、本机配置或凭据。许可证状态见 docs/provenance.md，不自行扩大来源授权。

研究工作区的联动维护由该工作区管理；克隆此仓库不要求读取或安装研究工作区工具。
