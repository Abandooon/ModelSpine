# 工程验收与结果边界

从仓库根运行 `python -B run_tests.py`；只使用 Python 3.10+ 标准库。离线贯穿示例为 `python -B apps/offline.py`，非法阈值反例为 `python -B apps/offline.py --threshold -1`，后者应明确失败并退出 2。

可用 `python -B run_tests.py --package protocols` 单独验收一个包，包名还可选 model-kernel、assurance、generation、requirements；`python -B run_tests.py --integration` 验收跨包路径。默认入口在六个隔离子进程中运行五包单元验收和集成验收，单包测试不经 offline CLI 加载其他能力包。

## 有意义的验收

| 边界 | 必须观察的行为 |
|---|---|
| 共享值对象 | 严格字段/类型/版本/哈希；bool 不冒充 int，未知字段不被忽略 |
| 模型提交 | 预览不写状态；权限、旧基准、错误决定和冲突失败；同一提案重试可识别 |
| 影响与证据 | 相关依赖失效、完整无关范围保留、缺失依赖 unknown；原报告不改写 |
| 检查器 | 报告属于实际候选/计划/范围/工具；不支持义务保留残余，伪造或错绑定报告拒绝 |
| 生成与修复 | 构造排除非法值，绕过构造的值仍须终验；改善不得掩盖新增错误、丢失覆盖或变更义务 |
| 模块解耦 | 生产导入符合实际运行依赖；实验/应用装配不得反向进入能力包 |

2026-09-21 历史验收：45 项全部通过，包括 protocols 7、model-kernel 7、assurance 4、generation 7、集成 20；CLI 正常路径退出 0，非法阈值退出 2。对应源码随该轮提交保存，后续修改必须按变更重新验收，不能继承此结果。

首轮切片曾记录 34 项行为测试通过，该数字只覆盖历史版本。新增内核测试用独立检查器证明接口可替换，未宣称实现新的完整领域；报告错绑定、遗漏/重复义务和执行器异常有拒绝反例。

测试入口和实际断言是本仓库可复查的工程依据；来源复用范围见 [provenance.md](provenance.md)。通过这些检查不证明形式化忠实、一般模型演化、任意应用生成或元模型中心方法的比较优势。

## 2026-09-22 公共基础边界复核

本轮完整验收 **69 项全部通过**：protocols 9、model-kernel 11、assurance 5、generation 9、集成 35。四包与集成在隔离子进程中执行；新增适配器可仅加载 protocols 导入。执行命令为 `python -B -I run_tests.py`。

两个 `python -B -I apps/boundary_examples.py --profile ...`（structural-graph / finite-automaton）均退出 0 并提交 revision 1；未知 profile 退出 2。既有 offline CLI 正常退出 0，非法阈值退出 2。完整运行日志、源文件哈希及对应提交在研究工作区本轮任务记录中保留；公开代码与这些可重跑入口随本次 Git 提交保存。

新增 24 项包括来源引用/空字段校验、同实例删除 ID 不复用、impact 比较域、字段控制声明与缺目标优先级，以及 14 项异质配置验收和 1 项适配器隔离导入检查。异质验收包含端点/环、非确定转移、实际输入串拒绝、Unicode 符号、未知义务、虚假依赖完整性、嵌套成员拒绝及其他根隔离。检查或授权失败保持模型不变；全图证据 unknown 和变更后的 stale 均实际观察。

独立审查曾复现嵌套成员被忽略后错误通过，已补拒绝及回归；最终结果包含此修复。自动机 CLI 的 trace 是可编辑模型数据，成功编辑不是保持固定独立需求的证明，详见[边界说明](foundation-boundaries.md)。本轮不增加研究运行或比较结论。

## 2026-09-23 固定任务合同与目标验收

`python -B -I run_tests.py` 完整运行 **126 项全部通过**：protocols 19、model-kernel 11、assurance 16、generation 9、集成 71；包含原 69 项回归和新增 57 项。`apps/task_acceptance.py --profile structural-graph` 与 `--profile finite-automaton` 均退出 0、保存 revision 1；未知 profile 退出 2。新增包/适配器隔离导入及应用不加载参考验收的检查均通过。

新增验收覆盖任务/来源原字节及版本绑定、精确映射、必需意图未决、无计划早退、固定图可达与输入接受/拒绝、非法模型不能满足负向期望、错误/未知保留、报告完整覆盖及提交复核异常不改变已接受快照/收据。两份正例只改名称并保持身份；行为反例另实际修改边、转移及输入，证明模型内部合法不等于固定任务目标满足。

刻意注入的错误开发检查器把候选自选 trace 的成功推广到全部目标；保持正确的任务、计划、候选、范围与义务绑定，经内核保存后，独立参考在固定 ab/aa 上均返回 violated。保存与参考失败两份结果均保留，不伪造回滚。它揭示绑定不能证明语义正确，不能记成自然失败率、论文效果或已验证的相关错误算法。

完整原日志、公开输入/源码哈希、共同错误复现脚本与对应 Git 提交由研究工作区本轮记录保存；公开仓库可由测试入口复验，不包含旧源码副本。按原字节固定的任务/评价夹具通过 `.gitattributes` 关闭换行转换，避免克隆时改变其 ArtifactRef。

参考验收来自同团队公开工程规格，且沿用明确的任务要求；不是隐藏测试集或独立专家真值。公共任务边界不证明自然语言意图完整、映射忠实或比较优势。该轮 generation 算法未改，五个方向模块当时继续 planned。

## 2026-09-26 有限澄清消费者

`python -B -I run_tests.py` 完整运行 **178 项全部通过**：protocols 19、model-kernel 11、assurance 16、generation 9、requirements 28、集成 95。保留此前 126 项并新增 52 项。两域 `apps/clarification.py --profile ...` 均退出 0、完成行为变更并提交 revision 1；未知 profile 退出 2。真实来源、案例、问题、原始回答、后继任务和报告按版本/哈希关联，见[运行说明](clarification.md)。

新增验收覆盖连续两轮 4→2→1 过滤、确定性分组选择、来源/案例/回答字节及身份、任务/快照/探针/候选集绑定、答案 ID 重用、严格 bool/int、拒答/冲突/缺证/未知与各项预算。无区分探针保持未决；无变化解释经后继目标评估但不产生提交。父声明、义务、绑定按值保留，后继版本不得复用父任务或父计划版本。

刻意注入的语义错误观察器保留正确绑定但翻转预测，澄清仍可收敛；实际候选在最终固定目标检查中被拒，未改变已接受快照。父目标与回答冲突也保留原目标并拒绝提交。测试侧预先编写的参考规格接受选定候选，并拒绝另一个虽满足父目标却违反回答目标的候选；参考结果不进入方法输入。观察与终验复用有限语义辅助，不能据此声称免疫共同错误。

requirements 实际只依赖 protocols/标准库；适配器及应用隔离检查通过。没有新增外部依赖、自然语言抽取、Jev/LLM 调用、时间预算或自动 representation_gap 证明。脚本回答与同团队参考规格是工程材料，不是实际用户实验或独立专家真值；P1/claims 保持研究假设。完整日志与当前代码身份登记于研究工作区本轮交接，代码由 Git 保存。

## 2026-09-27 有界构造与确定性机制先导

`python -B -I run_tests.py` 完整运行 **234 项全部通过**：protocols 19、model-kernel 11、assurance 16、generation 30、requirements 28、集成 130。原 178 项保留，新增 56 项。`apps/bounded_generation.py` 退出 0，考虑 3 项：构造前排除自环、无环但固定可达性失败、全部目标满足并保存 revision 1。完整回归和 CLI 运行前后源码/输入哈希一致，日志保存在研究工作区本轮交接。

新增验收覆盖预算/空间耗尽、无变化、unknown/error/不适用、选项/任务/基准/报告绑定、全义务终验、回调异常，以及搜索通过后最终接受仍可拒绝。DAG 控制真正先于提案创建，去掉旧边后判断回路，保留平行边。独立审查发现可编辑边的额外/不完整依赖可能被覆盖，已收窄支持并补反例；未编辑边原依赖和根 incomplete 声明保留。固定序号查找消除反复扫描选项，完整空间的准备成本仍明确保留。

[construction 工程先导](../studies/construction/README.md)实际保留 24 条计划/启动/终态：21 条三条件/预算对照及 3 条预先声明的故障探针。9 次找到并保存候选、6 次空间耗尽、6 次预算耗尽、1 次 unknown、2 次 error。23 次 app 正常返回；抛异常那次未返回保存状态，记录 null 而非推断回滚。全部 9 个保存模型通过独立参考。

预算 3 时，仅终验构造/检查 3 个候选，DAG 前控与独立普通规则均为 2 个；全部考虑相同 3 个选项、选定相同模型，保存阶段另有实测 3 次检查。普通规则与 DAG 控制在完整九选项上相同，所以此结果只支持当前机制可运行、前控省去一次候选构造/终验，不能归因为元模型组织方式的优势。参考表和来源判据事先固定，参考不参与生产搜索；人工/计算费用未测仍为 null。

C 仅重放来源身份关联与同元模型修复/新增见证，没有生产诊断器。缺少来源关联不能以一个可行修复强分 mapping_error/model_gap，有限无解或预算耗尽不证明 representation_gap。一个确定性工程任务、多预算和刻意故障不是独立统计样本，也不是 LLM/真人实验；claims、论文与 lifecycle 状态不升级。

## 2026-09-27 构造先导失败记录修复

修复 study 在 app 保存成功后遇到独立参考异常会丢失记录，以及整批延迟输出可能留下空文件的问题。参考异常与 app 保存事实分别记录；排他创建和原子 JSON 检查点保留已完成试次及当前已返回 app，后续异常明确非零退出，不重试搜索、不改写历史数据。此修改仅涉及研究记录层，生产包、A 构造接口和 C 诊断范围不变。

`python -B -I run_tests.py` **241/241 通过**：19/11/16/30/28/137。新增 7 项故障回归包括参考候选/保存评价异常、先保存 app 检查点、后续试次异常、序列化失败、已有文件拒绝覆盖、参考错误退出 2；文件保留断言使用真实临时目录。修复后[新的 24 条先导](../studies/construction/runs/2026-09-26T164942482157Z.json)退出 0、保存 9 个模型且参考均满足，源文件哈希前后一致。旧运行、旧测试日志与首次发布失败记录均保持原字节。运行正常和故障回归是不同证据，不将注入错误算作自然失败率或方法优势。

## 支持范围

当前模型仅含有限标量字段和显式依赖。事务只覆盖同一 ModelKernel 实例的内存状态，不覆盖磁盘、跨进程、构建或应用数据。assurance 内置领域义务仍为 integer_range/equals，另提供通用固定任务准备/评估；显式适配器另解释结构图和有限自动机配置的有限规则，见[支持矩阵](foundation-boundaries.md)。CLI 不执行真实业务批准，也不调用 LLM、外部求解器或付费 API。

既有订单模型是工程夹具；具体金额、币种与角色不成为通用默认。一般行为合同形式化、代码恢复、界面、文件所有权和应用交付仍需对应实现及独立验收。研究比较的条件、真值、失败分母和成本要求见[协议摘要](../studies/lifecycle/protocol.md)。
